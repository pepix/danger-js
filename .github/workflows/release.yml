name: Release executable files for macOS

on:
  workflow_dispatch:
    inputs:
      version_digit:
        description: 'Version digit (major | minor | patch)'
        required: true
        default: 'patch'
        type: choice
        options:
        - major
        - minor
        - patch

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Install ldid
        uses: MOZGIII/install-ldid-action@v1
        with:
          tag: v2.1.5-procursus6

      - name: Check out repository code
        uses: actions/checkout@v3
        with: # Remove this
          ref: test-release-it
      
      - name: Use node
        uses: actions/setup-node@v3
        with:
          node-version: "14"

      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build

      - name: Package
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Action"
          npm run release -- ${{ github.event.inputs.version_digit }} --ci

      - name: Show env
        run: env
      
      - name: Run shell script (1)
        shell: bash
        run: echo "scripts/create-homebrew-tap-pr.sh $VERSION"

      - name: Run shell script (2)
        shell: bash
        run: scripts/create-homebrew-tap-pr.sh ${{ env.VERSION }}
        env:
          VERSION: ${{ env.VERSION }}

      - name: Check ruby file
        run: cat danger-js.rb